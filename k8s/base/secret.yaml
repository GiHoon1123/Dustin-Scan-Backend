apiVersion: v1
kind: Secret
metadata:
  name: dustin-scan-secret
  namespace: dustin-scan
type: Opaque
data:
  # base64로 인코딩된 비밀번호
  # 실제 값: "1234"
  DB_PASSWORD: MTIzNA==

  # base64로 인코딩된 데이터베이스 이름 (overlay에서 변경)
  # 실제 값: "dustin_scan"
  DB_DATABASE: ZHVzdGluX3NjYW4=
