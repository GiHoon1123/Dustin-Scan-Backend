# ============================================
# Dustin Scan Backend - Environment Variables
# ============================================
# 이 파일을 복사해서 .env.development 또는 .env.production 으로 사용하세요
# 예: cp .env.example .env.development

# ============================================
# 실행 환경
# ============================================
NODE_ENV=development # development | production

# ============================================
# 서버 포트
# ============================================
API_PORT=4000
INDEXER_PORT=4001
SYNC_PORT=4002

# ============================================
# 데이터베이스 설정 (PostgreSQL)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=root
DB_PASSWORD=1234
DB_DATABASE=dustin_scan_dev # development: dustin_scan_dev, production: dustin_scan
DB_SYNCHRONIZE=true # production에서는 반드시 false!

# ============================================
# Dustin-Chain RPC URL
# ============================================
# Sync, API, Indexer가 체인 RPC를 호출할 때 사용
CHAIN_URL=http://localhost:3000

# ============================================
# Indexer Server URL (Sync에서 사용)
# ============================================
# Sync 서버가 Indexer 서버에 블록 데이터를 전달할 때 사용
# Kubernetes 환경: http://indexer-service:4001
# 로컬 환경: http://localhost:4001
INDEXER_URL=http://localhost:4001
